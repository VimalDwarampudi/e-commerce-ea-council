---
type: Metric
title: cart-abandonment-rate
description: Share of created carts that don't convert to an order; a primary checkout health metric.
tags: [commerce-checkout, metric, placeholder]
timestamp: 2026-07-24T00:00:00Z
status: PLACEHOLDER
---

# cart-abandonment-rate

**Definition:** `1 - (count(carts with order_id) / count(all carts created in period))`

**Target (placeholder):** directional only — retail benchmarks vary widely by vertical; track trend, not an absolute target, and segment by channel to catch store/digital divergence early (see the channel-parity enterprise principle).

> **PLACEHOLDER — sample only.** This concept illustrates the shape an asset concept should take once a live source system is connected (see `CLAUDE.md` "Data Sources"). Field names, endpoint paths, and metric values below are representative, not verified against a real system. Replace the body with the actual schema/contract/metric definition and remove `status: PLACEHOLDER` from the frontmatter when done.
